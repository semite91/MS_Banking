Account Service (scaffold)

Features:
- .NET 8 Minimal API scaffold
- Health endpoint
- Swagger
- Dockerfile

How to run locally:

```bash
cd account
dotnet run
```

Build Docker image:

```bash
docker build -t bank-account:latest .
```
